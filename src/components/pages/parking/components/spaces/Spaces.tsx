import styles from './spaces.module.scss'

const Spaces = () => {
  return (
    <div className={styles['block-wrapper']}>
      <div className={styles['title-wrapper']}>
        <h1 className={styles['title']}>Парковочные места</h1>
      </div>
    </div>
  )
}

export default Spaces
