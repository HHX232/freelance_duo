version: '3'

services:
  frontend:
    build:
      context: .
    image: registry.gitlab.com/soundsnick/kronfort:${TAG:-latest}
    restart: unless-stopped
    ports:
      - "4000:3000"

networks:
  default:
    driver: bridge
    driver_opts:
      com.docker.network.driver.mtu: 1450